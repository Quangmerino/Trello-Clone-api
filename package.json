{
  "name": "trello-clone-api",
  "version": "1.0.0",
  "description": "QuangOc",
  "main": "src/index.js",
  "type": "commonjs",

  "scripts": {
    "start": "nodemon --inspect src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "devDependencies": {
    "morgan": "^1.10.0",
    "nodemon": "^2.0.22"
  },

  "dependencies": {
    "express": "^4.18.2"
  }
}
